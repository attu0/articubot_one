# Ros pkg to install to make it workinh

```bash
sudo apt install ros-humble-rplidar-ros #for lidar is you are using rpa lidar
sudo apt install ros-humble-ros2-control ros-humble-ros2-controllers ros-humble-gazebo-ros2-control #for ros2 contol
sudo apt install libraspberrypi-bin v4l-utils ros-humble-v4l2-camera ros-humble-image-transport-plugins ros-humble-rqt-image-view # for camera 
```